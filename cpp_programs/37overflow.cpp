#include<iostream>
using namespace std;
#include <cmath>
#include <climits> // we used this because INT_MAX is defined in this header file

int main() {
 /*  char x=127;
    x++;
    x--;
    cout<<(int)x<<endl;*/
    int x= INT_MAX;
    x++;
    cout<<x<<endl;
}